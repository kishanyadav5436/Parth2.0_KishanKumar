# This project currently uses Node.js and TypeScript.
# Python requirements are not needed here, so this file is a placeholder.
# Use package.json and package-lock.json / pnpm-lock.yaml for JS dependencies.
